import { Flex } from '@chakra-ui/react'
import React from 'react'
import Cards from '../../components/Cards'

const Shop = () => {
  return (
    // <Flex><Cards title={product.title} id={product._id} price={product.price}/></Flex>
    <div>Shop</div>
  )
}

export default Shop